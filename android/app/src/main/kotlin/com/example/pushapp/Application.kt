package com.example.pushapp

import io.flutter.app.FlutterApplication

class Application : FlutterApplication() {
    override fun onCreate() {
        super.onCreate()
        // TaskHandler is now defined in Dart code, not here
    }
}
